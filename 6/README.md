# Лабораторная работа 6 — Кластерный анализ корпоративной отчётности

**Данные:** 22 финансовых показателя по 1 717 компаниям, годы **2007 и 2008**  
**Метод:** PAM (Partitioning Around Medoids), оптимальное k выбирается по силуэту  
**Скрипт:** [`lab_06.R`](lab_06.R)

---

## Консольный вывод

После загрузки 12 CSV-файлов (1 717 строк в каждом) скрипт формирует 6 финансовых коэффициентов (Леверидж, ROCE, Гудвилл, Фин.Активы, Байбек, ROS), объединяет два года и удаляет наблюдения с NA/Inf и выбросами. Итоговая выборка — **3 078 наблюдений** (1 547 за 2007 г. и 1 531 за 2008 г.).

Перебор k от 2 до 8 даёт следующие средние значения силуэта:

| k | 2 | 3 | 4 | **5** | 6 | 7 | 8 |
|---|---|---|---|---|---|---|---|
| avg sil | 0.167 | 0.204 | 0.183 | **0.211** | 0.197 | 0.203 | 0.168 |

Максимум при **k = 5** — выбрано как оптимальное число кластеров.

Распределение компаний по кластерам: **611 / 1138 / 671 / 421 / 237**.

Медианные профили кластеров показывают осмысленное разделение:
- **Кластер 3** — высокое ROCE (0.16) и ROS (0.27): прибыльные компании с низким долгом
- **Кластер 4** — высокий Гудвилл (0.95): компании с крупными M&A
- **Кластер 5** — значимая доля байбека (0.47): активный возврат капитала акционерам
- **Кластер 2** — наибольший размер (1138): «типичные» умеренно-задолженные компании
- **Кластер 1** — умеренный леверидж с долей финансовых активов выше средней

---

## Графический вывод

### 01 — Выбор числа кластеров (силуэт)

![01_silhouette_k_selection](output/01_silhouette_k_selection.png)

График показывает, как меняется средний коэффициент силуэта при переборе числа кластеров от 2 до 8. По оси X отложено число кластеров, по оси Y — среднее значение силуэта, то есть мера того, насколько хорошо объекты отделены от соседних групп и насколько компактны внутри своей группы. Красная пунктирная линия отмечает выбранное значение **k = 5**.

Смысл графика в том, что он служит формальным критерием выбора числа кластеров. Чем выше средний силуэт, тем лучше одновременно выполняются два условия: объекты внутри кластера похожи друг на друга, а разные кластеры достаточно различимы. Здесь максимум достигается при **k = 5** со значением около **0.211**, тогда как при **k = 4** и **k = 8** качество заметно хуже.

Вывод по графику: разбиение на **5 кластеров** является наилучшим среди проверенных вариантов, хотя само значение силуэта нельзя назвать очень высоким. Это означает, что структура в данных есть, но кластеры разделены умеренно, без абсолютно чётких границ.

---

### 02 — Силуэтный график итоговой кластеризации

![02_silhouette_final](output/02_silhouette_final.png)

На этом графике каждая горизонтальная полоска соответствует отдельному наблюдению, а длина полоски равна его индивидуальному коэффициенту силуэта. Наблюдения сгруппированы по пяти кластерам, поэтому можно визуально оценить качество не только всей кластеризации в целом, но и каждого кластера по отдельности. Среднее значение по всей выборке составляет около **0.21**.

Смысл графика в диагностике качества итогового разбиения. Если большинство полос длинные и лежат справа от нуля, кластеризация удачна; если много коротких или отрицательных значений, значит часть объектов находится на границе между кластерами или отнесена неоднозначно. Здесь лучше всего выглядит **кластер 2**: он самый крупный и имеет высокое среднее качество отделения. **Кластер 5** тоже отделён сравнительно неплохо. Наиболее слабым выглядит **кластер 4**, где значения силуэта меньше и ближе к нулю.

Вывод по графику: итоговая модель с пятью кластерами работоспособна, но качество кластеров неодинаково. В данных есть как хорошо выраженные группы, так и более размытые профили компаний, особенно у кластера 4.

---

### 03 — Рассеяние: Леверидж vs ROS

![03_scatter_L_vs_P](output/03_scatter_L_vs_P.png)

Диаграмма рассеяния показывает распределение компаний в координатах **леверидж** и **ROS**. Цветом отмечен кластер, а формой точки — год наблюдения: круги соответствуют 2007 году, треугольники — 2008 году. Таким образом, на одном графике одновременно видны и финансовые различия между компаниями, и возможные сдвиги между двумя годами.

Смысл графика в том, чтобы проверить, различаются ли кластеры по двум важным характеристикам: долговой нагрузке и рентабельности продаж. Видно, что основная масса компаний сосредоточена в зоне сравнительно невысокого левериджа и умеренного ROS. При этом отдельные кластеры смещаются в разные области: **кластер 3** чаще лежит выше по оси ROS, то есть включает более рентабельные компании, а **кластер 4** заметнее вытянут вправо по оси левериджа и связан с более высокой долговой нагрузкой.

Вывод по графику: кластеры не разделяются полностью в двумерной проекции, но различия по прибыльности и долговой нагрузке всё же наблюдаются. Кроме того, точки 2007 и 2008 годов сильно перемешаны, значит базовая структура кластеров между этими годами в целом сохраняется.

---

### 04 — Рассеяние по регионам: Леверидж vs ROCE

![04_scatter_by_region](output/04_scatter_by_region.png)

График разбивает те же компании по регионам и показывает в каждом регионе связь между **левериджем** и **ROCE**. Цвет по-прежнему обозначает кластер, а форма — год. Такое фасетирование позволяет увидеть, одинаково ли проявляются найденные кластеры в разных частях мира или же отдельные кластеры характерны только для отдельных регионов.

Смысл этого графика в сравнении географической структуры кластеров. Наиболее плотное и разнообразное облако наблюдений видно в **USA** и **EU** — именно там представлено больше всего компаний и лучше заметны все пять кластеров. В **Japan** заметна высокая концентрация точек вблизи низких значений ROCE, причём там особенно велик вклад **кластера 2**. В **India**, **Russia**, **Latin** и некоторых других регионах наблюдений меньше, поэтому структура выглядит менее насыщенной, но базовые типы компаний всё равно повторяются.

Вывод по графику: кластеры нельзя свести только к региональному делению. Хотя регионы отличаются по плотности и по преобладающим типам компаний, одна и та же кластерная структура встречается в нескольких географических зонах, то есть она отражает скорее финансовые профили компаний, чем просто их местоположение.

---

### 05 — Рассеяние по секторам: Леверидж vs ROS

![05_scatter_by_sector](output/05_scatter_by_sector.png)

Здесь показано распределение компаний по секторам экономики в координатах **леверидж** и **ROS**. Каждый малый график соответствует отдельному сектору, поэтому можно сравнивать, как именно кластеры проявляются в отраслях с разной бизнес-моделью, рентабельностью и структурой капитала.

Смысл графика в проверке того, не являются ли найденные кластеры просто отражением отраслевой принадлежности. Видно, что в секторах **Oil**, **Metallurgy** и **Energetics** заметна большая доля **кластера 3**, который связан с более высокими значениями ROS. В **Transport** особенно велик вклад **кластера 2**, а в **Medicine**, **Telecom** и частично **Retail** структура более смешанная: там присутствуют сразу несколько кластеров, включая группы с более высокими значениями левериджа или ROS.

Вывод по графику: отраслевой фактор влияет на распределение кластеров, но не определяет его полностью. Некоторые сектора действительно тяготеют к определённым финансовым профилям, однако в большинстве отраслей наблюдается смесь нескольких кластеров, следовательно кластеризация отражает не только сектор, но и внутренние различия компаний внутри него.

---

### 06 — Параллельные координаты

![06_parallel_coords](output/06_parallel_coords.png)

График параллельных координат показывает все шесть стандартизованных признаков одновременно: **L**, **R**, **G**, **F_val**, **T_val** и **P**. Тонкие линии соответствуют отдельным компаниям, а жирные линии — медианным профилям кластеров. Панели разделены по годам, поэтому можно сравнивать форму профилей в 2007 и 2008 годах.

Смысл такого графика в многомерной интерпретации кластеров. Он позволяет увидеть, какой набор признаков характерен для каждой группы, а не анализировать переменные по одной. Наиболее заметны два профиля: **кластер 4** выделяется высоким значением **G** — доли гудвилла, а **кластер 5** резко поднимается по **T_val**, то есть связан с байбеком. **Кластер 3** имеет повышенные значения **R** и **P**, что соответствует более высокой рентабельности, тогда как **кластер 1** отличается увеличенным **F_val**.

Вывод по графику: кластеры имеют содержательно разные многомерные профили, и эти профили почти не меняются между 2007 и 2008 годами. Это подтверждает, что найденное разбиение устойчиво и интерпретируемо с экономической точки зрения.

---

### 07 — Матрица рассеяния (все пары признаков)

![07_pairs_matrix](output/07_pairs_matrix.png)

Матрица попарных диаграмм рассеяния показывает все возможные двумерные проекции шести признаков. По диагонали подписаны сами переменные, а вне диагонали находятся облака точек, раскрашенные по кластерам. Такой формат позволяет увидеть, в каких именно сочетаниях признаков кластеры различаются лучше всего.

Смысл графика в комплексной визуальной проверке разделимости кластеров. По матрице хорошо видно, что наиболее отчётливое отделение возникает в проекциях, где участвуют **G (гудвилл)** и **T_val (байбек)**: соответствующие кластеры образуют сравнительно обособленные облака. Напротив, в проекциях с **L**, **R** и **P** наблюдается заметное перекрытие точек разных цветов, поэтому по одной-двум этим координатам полностью разделить компании нельзя.

Вывод по графику: кластеризация опирается не на один доминирующий показатель, а на сочетание нескольких признаков. При этом признаки, связанные с гудвиллом и байбеком, дают наиболее сильный вклад в визуальное разделение отдельных кластеров, тогда как показатели долговой нагрузки и рентабельности чаще создают частично перекрывающиеся группы.

---

## Теоретические сведения

### Что такое кластерный анализ

Кластерный анализ — это метод многомерной статистики и машинного обучения без учителя, предназначенный для разбиения объектов на **группы (кластеры)** так, чтобы объекты внутри одного кластера были максимально похожи друг на друга, а объекты из разных кластеров — максимально различались. В отличие от классификации, при кластеризации заранее неизвестны правильные метки классов: структура данных выявляется непосредственно по значениям признаков.

В контексте корпоративной отчётности кластеризация позволяет выделять типовые профили компаний: например, устойчиво прибыльные фирмы, компании с высоким уровнем долговой нагрузки, фирмы с крупным гудвиллом после сделок M&A или компании, активно возвращающие капитал акционерам через buyback. Такое разбиение полезно для финансового анализа, сегментации эмитентов и поиска скрытых закономерностей в структуре показателей.

### Пространство признаков и расстояния

Каждая компания в задаче представляется точкой в многомерном пространстве признаков. Если для анализа выбраны 6 финансовых коэффициентов, то каждая строка данных интерпретируется как точка в шестимерном пространстве. Основная идея кластеризации состоит в сравнении расстояний между такими точками: близкие точки считаются похожими по финансовому профилю, а удалённые — различными.

На практике качество кластеризации существенно зависит от подготовки признаков. Финансовые показатели могут иметь разные масштабы, тяжёлые хвосты распределения и выбросы, поэтому перед кластеризацией часто выполняют:

- очистку пропусков и некорректных значений;
- удаление или ослабление влияния выбросов;
- нормализацию/стандартизацию признаков;
- переход от абсолютных величин к относительным коэффициентам.

Использование коэффициентов вместо абсолютных показателей особенно важно для корпоративной отчётности, поскольку компании различаются по масштабу бизнеса, и без нормировки размер фирмы может доминировать над экономическим содержанием остальных признаков.

### Метод PAM (Partitioning Around Medoids)

В работе используется алгоритм **PAM** — Partitioning Around Medoids. Он относится к методам разбиения, как и k-means, однако вместо центроидов использует **медоиды**. Медоид — это реальный объект выборки, который является наиболее «центральным» представителем своего кластера, то есть минимизирует суммарное расстояние до остальных объектов внутри группы.

Основные идеи PAM:

1. выбирается число кластеров k;
2. среди наблюдений подбираются k медоидов;
3. каждый объект относится к ближайшему медоиду;
4. далее медоиды итеративно уточняются так, чтобы уменьшить суммарную внутрикластерную неоднородность.

Преимущества PAM по сравнению с k-means:

- устойчивость к выбросам, поскольку центр кластера всегда является реальным наблюдением;
- возможность применять произвольные меры расстояния;
- более интерпретируемые «представители» кластеров.

Для финансовых данных это особенно удобно, так как распределения коэффициентов часто не являются нормальными, а отдельные компании могут иметь экстремальные значения.

### Выбор числа кластеров

Одной из ключевых задач кластерного анализа является выбор числа кластеров **k**. Если кластеров слишком мало, различающиеся типы компаний будут искусственно объединены; если слишком много — модель начнёт дробить естественные группы на мелкие и слабо интерпретируемые подмножества.

В данной лабораторной работе число кластеров определяется по **среднему коэффициенту силуэта**. Для каждого наблюдения вычисляется значение:

\[
s(i) = \frac{b(i) - a(i)}{\max(a(i), b(i))}
\]

где:

- \(a(i)\) — среднее расстояние от объекта до остальных объектов своего кластера;
- \(b(i)\) — минимальное среднее расстояние до объектов ближайшего соседнего кластера.

Интерпретация силуэта:

- **s(i) близко к 1** — объект хорошо соответствует своему кластеру;
- **s(i) около 0** — объект находится на границе между кластерами;
- **s(i) < 0** — объект, вероятно, отнесён не к тому кластеру.

Среднее значение силуэта по всей выборке служит формальным критерием качества разбиения. Чем оно выше, тем лучше одновременно достигаются компактность кластеров и их разделимость.

### Визуальная интерпретация кластеров

Формального критерия недостаточно для полноценного анализа, поэтому кластеризацию обычно дополняют визуальными методами. В лабораторной работе используются:

- силуэтные диаграммы;
- диаграммы рассеяния по парам признаков;
- фасетированные графики по регионам и секторам;
- параллельные координаты;
- матрица попарных проекций.

Такие представления позволяют оценить, какие именно признаки сильнее всего разделяют компании, насколько устойчиво разбиение между годами и существуют ли содержательно интерпретируемые типы фирм. Если кластер можно описать через экономический смысл (например, «высокий ROCE и низкий долг» или «высокий гудвилл после поглощений»), то результат кластеризации становится полезным для прикладного финансового анализа.

### Экономический смысл кластеризации компаний

Для корпоративной отчётности кластерный анализ удобен тем, что позволяет перейти от набора разрозненных коэффициентов к типологии компаний. Вместо анализа каждого признака по отдельности исследователь получает группы с близкими стратегиями финансирования, рентабельности и структуры активов. Это помогает:

- сравнивать компании не по одному показателю, а по целому профилю;
- искать атипичные фирмы и потенциальные выбросы;
- анализировать устойчивость финансовых моделей по годам;
- выделять сегменты для дальнейшего инвестиционного или отраслевого исследования.

Таким образом, кластерный анализ в данной лабораторной работе выступает как инструмент структурирования сложных многомерных данных корпоративной отчётности и выявления скрытых экономических типов компаний на основе их финансовых коэффициентов.
